<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('includes/head') %>
    <title>Edit</title>
</head>
<body>
    <%-include('includes/header') %>
    <main>
        <div class="container">
            <form action="/edit" method="post">
                <div class="form-gruop">
                    <label >제목</label>   
                    <input type="text" name="id" value="<%= data._id %>" class="form-control" style="display: none;">
                    <input type="text" name="title" value="<%= data.title %>" class="form-control">
                </div>
                <p></p>
                <div class="form-gruop">
                    <label for="">내용</label>  
                    <textarea name="content" rows="10" value="<%= data.content %>" class="form-control"></textarea>
                </div>
                <p></p> 
                <div class="form-group">
                    <label for="">작성일</label>
                    <input type="date" class="form-control" value="<%= data.someDate %>" name="someDate">
                </div>
                <p></p>
                <button type="submit" class="btn btn-warning" style="float:right">저장</button>
            </form>
        </div>
    </main>
</body>
</html>